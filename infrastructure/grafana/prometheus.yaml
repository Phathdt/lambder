# Prometheus — scrapes the otel-collector's /metrics exposition endpoint.
global:
  scrape_interval: 10s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ['otel-collector:8889']
