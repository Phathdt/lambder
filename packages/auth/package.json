{
  "name": "@lambder/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/infrastructure/db/auth.schema.ts",
    "./module": "./src/auth.module.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -b",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@lambder/cache": "workspace:*",
    "@lambder/contracts": "workspace:*",
    "@lambder/db": "workspace:*",
    "@lambder/shared-kernel": "workspace:*",
    "@node-rs/argon2": "^2.0.2",
    "drizzle-orm": "^0.36.0",
    "ioredis": "^5.4.0",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@lambder/tsconfig": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
