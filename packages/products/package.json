{
  "name": "@lambder/products",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/infrastructure/db/products.schema.ts",
    "./module": "./src/products.module.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -b",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@lambder/contracts": "workspace:*",
    "@lambder/db": "workspace:*",
    "@lambder/shared-kernel": "workspace:*",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@lambder/tsconfig": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
