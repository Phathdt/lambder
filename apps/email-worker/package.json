{
  "name": "email-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/dev-server.ts",
    "build": "rolldown -c rolldown.config.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@lambder/contracts": "workspace:*",
    "@lambder/email": "workspace:*",
    "@lambder/shared-kernel": "workspace:*",
    "dotenv": "^17.4.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@lambder/tsconfig": "workspace:*",
    "@types/aws-lambda": "^8.10.145",
    "rolldown": "1.0.0-rc.18",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
